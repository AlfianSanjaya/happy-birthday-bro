{
  "name": "happy-birthday-bro",
  "version": "1.0.0",
  "scripts": {
    "start": "sirv ./src public --cors --single --no-clear --port 8000",
    "build": "tailwindcss build src/styles.css -o public/styles.css"
  },
  "dependencies": {
    "tailwindcss": "^2.2.19",
    "postcss": "^8.4.5",
    "autoprefixer": "^10.4.2",
    "sirv-cli": "^1.0.0"
  },
  "devDependencies": {
    "postcss-cli": "^9.1.0"
  }
}